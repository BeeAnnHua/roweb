彼岸花仙境 RO_WEB V0.9.86O
Legacy 已復原角色登入相容修正

基準：V0.9.86N / roweb(4).zip

修正：
1. 修正早期固定槽位背包含 null 空格時，normalizePlayerData 直接讀 item.id 導致 Loading 中斷。
2. 相容舊 inventory object / items wrapper / itemId / amount / qty / quantity 欄位，盡量保留舊物品與數量。
3. 相容舊 skills -> learnedSkills 與 quickbar/hotkeys -> quickSlots 的基本結構遷移。
4. index.html 將所有本地 JS cache key 統一提升到 ?v=0.9.86O，確保朋友瀏覽器不再沿用舊 player.js / skill_engine.js。
5. 保留目前 skill_engine.js 內「樞機主教舊 Buff 載入避免 Maximum call stack」修正，並藉由新 cache key 強制真正載入。
6. 若仍有舊資料格式異常，不再無限停在 Loading：會顯示具體錯誤訊息並安全返回角色選擇，方便截圖定位。
7. 不需要新增或重跑 Supabase SQL；沿用 V0.9.86I RPC。
8. 不刪除 localStorage / IndexedDB Legacy 原始資料，也不自動刪除任何已復原 ro_characters。

測試順序：
A. 套用 Patch 後完整重新整理頁面。
B. 先測 SLOT 2 樞機主教，再測 SLOT 3、SLOT 4。
C. 若能進遊戲，確認背包、裝備、技能、等級、地圖。
D. 若跳出「舊角色資料升級失敗」視窗，直接截圖回報文字即可。
