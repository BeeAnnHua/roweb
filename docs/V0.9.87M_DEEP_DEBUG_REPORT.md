# RO_WEB V0.9.87M — Double-Pass Deep Debug Report

Baseline: `roweb(20260812-160454).zip` (V0.9.87L)

## Round 1 — Full structure / integrity audit

- Runtime JS: 69 files; all pass `node --check`.
- Entire project JS (including tools/tests): 873 files; 0 syntax errors via `tools/check_all_js_syntax.js`.
- JSON: 2023 files parsed; 0 invalid JSON.
- Images: 5538 PNG/JPG/WebP/GIF decode/verify checks; 0 corrupt images.
- HTML: no duplicate IDs; no missing literal local script/style/image refs.
- CSS: no missing literal local `url(...)` refs.
- Data bundle consistency:
  - `js/data_bundle_core.js`: 309 entries; 0 missing source; 0 value mismatch.
  - `js/data_bundle.js`: 1541 entries; 0 missing source; 0 value mismatch.
- Large-file duplicate hash audit (>1 MiB): 0 duplicate groups.
- Broken symlinks: 0.
- Empty files excluding `.nojekyll`: 0.
- Supabase SQL: SECURITY DEFINER functions use explicit `search_path`; no broad direct table grants to `anon/public` found in active migrations.

## Round 1 findings fixed in M

1. Current version markers had drifted (some UI still showed F while runtime was J/L). Normalized current user-facing/runtime marker to M.
2. Supabase SDK was loaded from floating `@2` on a single external CDN. Pinned the tested SDK version and added the alternate official CDN as a parser-time fallback.
3. OFFLINE mode blocked mail claiming, but several secondary mail actions could still try cloud RPCs. Added OFFLINE guards for read/delete/GM-center cloud actions.
4. Both gacha inventory guards scanned the complete inventory every 1.5 s even while auto battle was off. Idle scans are now zero; while auto battle is active each guard performs one normal scan per interval.
5. Root release notes had begun accumulating again. Historical APPLY/INSTALL/test-report files were archived under `docs/release_history/`; root reduced to 21 items.

## Round 2 — Cross-check / targeted functional audit

- Gacha guard unit simulation:
  - idle: 0 full-inventory scans across repeated guard ticks;
  - active: 1 scan per guard tick;
  - stop auto battle: scanning stops again.
- OFFLINE mail unit simulation:
  - delete-read while OFFLINE: blocked before RPC;
  - GM CENTER while OFFLINE: blocked before RPC;
  - cloud RPC count in test: 0.
- Supabase Offline Continuity SQL rechecked:
  - `ro_resume_offline_character` is SECURITY DEFINER + `search_path=public`;
  - public execute revoked; authenticated execute granted;
  - expected cloud revision is still part of the atomic resume contract.
- MVP OOM protection from I remains present: bounded decoded-atlas cache, offscreen reference release, map-change cache cleanup, stale async-load protection, slim reward snapshots.
- Auction / warehouse / refine / enchant / account switching OFFLINE guards remain present after M changes.
- No new Supabase migration is required for M.

## Non-actionable / historical audit notes

`tools/deep_health_check.py` is a historical 0.9.82HX-era checker. It reports obsolete version/report expectations and dynamic DOM IDs as failures; those are not current-runtime failures. Its generic counts remain useful, but its version-specific PASS/FAIL result should not be used as the current release gate.

## Release recommendation

V0.9.87M is suitable as the next clean full baseline. Continue normal gameplay regression tests (login, CLOUD/OFFLINE toggle, auction, warehouse, mail, MVP arena) after deployment.
