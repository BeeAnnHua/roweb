# 彼岸花仙境（RO_WEB 技術核心） V0.9.88B4

## V0.9.88B4 — 自動分解選取操作修正

- 背包「自拆」改名為「自動分解」，並加寬操作按鈕。
- 修正 `item_instance_ui.js` 覆寫背包點擊處理後，漏掉自動分解模式的問題。
- 自動分解模式中點擊任何背包物品，現在會直接勾選／取消該 Item ID，不再開啟物品介紹視窗。
- 保留原有每 30 分鐘批次分解、鎖定／穿戴／受保護物品排除與按 Item ID 持久保存規則。
- 無 Supabase Schema / RPC 變更，不需要 SQL。

## V0.9.88B3 — 新手資源初始化 / OOM Leak 根因修正與診斷

- 新角色第一次進入初學者修練場前，先顯示「正在初始化遊戲資源……／正在確認最新資料版本，請稍候。」並把修練場必要靜態資源下載到瀏覽器快取；必要檔案未成功就不讓玩家先進黑畫面。
- Starter Pack 只預先下載／快取 bytes；怪物 Atlas 不會一次全部解碼進 RAM。共同資源約 2.9 MB，加上當前性別初學者資源約 0.6 MB。
- 找到一個會隨歷史戰鬥次數線性成長的明確 Leak：技能特效 `lastHit` 以每隻怪物唯一 instance key 記錄、同地圖長掛時原本永不 prune。B3 改為 5 秒 retention + 4096 hard cap，並同步讓 `latestTarget` 的死亡怪物引用 5 秒後釋放。
- 世界怪物、Bitmap、DOM、技能特效、Damage DOM、Reward Queue、Ground Effect、傭兵加入建立／銷毀／目前存活診斷，讓下一次長掛能判斷是否還有第二個 Leak。
- 世界怪物每次銷毀 Canvas 時先清空並壓回 1×1，再解除 DOM/Context；同一張怪物 Atlas 雖然會共用，但每個怪物實體自己的 Canvas backing store 也必須主動釋放。
- 背景分頁若 `requestAnimationFrame` 被瀏覽器暫停，Skill Effect 過期 instance 改由 safety poll 清理；Monster Impact target Set 加 timer fallback 與 hard cap，避免歷史怪物物件被畫面批次器抓住。
- Chat message duplicate Set 加 512 筆上限，順手消除另一個長開分頁的無界集合。
- Legacy Monster Atlas singleton cache 改為最多 12 項 LRU，降低跨舊地圖一路看過不同怪物後永久常駐的風險。
- 修復本次使用者完整 ZIP 的封裝遺漏：補回 `mercenary_runtime.js`、`mercenary.css`，並還原 B/B1 對 map / position / skill 的正式版本；B2 既有 ImageBitmap 回收與 30 分鐘自拆全部保留。
- 無 Supabase Schema / RPC 變更，不需要 SQL。


## V0.9.88B2 — 長時間掛機 Memory Recycle / 30 分鐘自動分解

- MVP Atlas 在 GitHub Pages / HTTP(S) 優先使用可明確 `close()` 的 ImageBitmap，搭配 2.5ms/frame 漸進 recycle queue。
- MVP 場每 400 擊殺或 10 分鐘做 Soft Sweep，不做一次性大 GC；畫面中的資源不碰。
- 背包「自拆」進入標記模式，勾選物品種類後，只要自動掛機連續滿 30 分鐘就安全批次分解一次。
- 停止掛機後重新計時；鎖定／穿戴／受保護道具不會被拆。
- 沒有新增資料庫 Schema / RPC，不需要 SQL。



## V0.9.88B1 — 傭兵平滑移動／玩家同尺寸／RO Atlas 基本動畫
- 傭兵視覺尺寸改為即時同步目前玩家 `player-sprite`，不再使用縮小版召喚物尺寸。
- AI 邏輯 Tick 維持 B 版低頻率；位置更新以 CSS linear transition 平滑補間，避免貼紙式跳格移動。
- 傭兵加入共用 Atlas Loader，Idle / Walk 預載，Attack / Hurt / Dead 依需求延遲載入；共用 LRU 最多 12 個動畫資源，避免重複解碼擴張。
- 加入 Idle、Walk、Attack、Hurt、Dead 基本角色動畫；攻擊方向會朝向目前目標。
- 解散／換隊時釋放傭兵對動畫資源的 Runtime 引用；瞬移與 1 秒 RESYNC 保留。
- B 版攻擊／輔助模式、300px 搜尋、450px 追擊、650px 拉回、HP/SP 每秒 1% 全部保留。

## V0.9.88B — 傭兵移動／模式 AI

- 每隻傭兵可獨立設定「攻擊」或「輔助」，並可決定遭攻擊／隊伍受威脅時是否反擊。
- 攻擊模式會以隊長為中心主動搜尋附近怪物、移動到適合射程、擊殺後返回隊長。
- 輔助模式只跟隨並守在隊長周圍，下一階段再接 Heal / Buff / Resurrection。
- 玩家使用蒼蠅翅膀、瞬移技能或切換地圖後，傭兵會清除舊位置並於約 1 秒後在新座標重新出現。
- 沿用每秒 MaxHP / MaxSP 1% 自然恢復與 30 秒自動復歸。


## V0.9.88A1 — 傭兵選單 Hotfix / 1% HP-SP 自然恢復

- 修正傭兵選單下拉後被 500ms UI 重繪立即關閉。
- 傭兵選隊面板只在視窗開啟或手動重新整理時更新。
- HP / SP 統一每秒回復最大值 1%，先以簡單規則進行實測。
- V0.9.88A 其餘傭兵 Snapshot／跟隨／基本攻擊／30 秒復歸安全設計全部保留。

## V0.9.88A — 分身傭兵核心

- 同帳號最多 3 名分身傭兵；只讀安全戰鬥快照，不碰原角色背包／經濟／雲端存檔。
- 新增選隊視窗、隊伍 HUD、跟隨、共享目標基本攻擊、HP/SP 與 30 秒復歸骨架。
- 下一階段將以此 Runtime 接入服事系單體 Heal / Buff / Resurrection 與群體輔助技能。

## V0.9.87N — MVP 試煉場 22 王精簡／長時間掛機記憶體穩定化

- 以 V0.9.87M 雙輪深度 DEBUG 完整基準為底。
- 葛坡尼亞 MVP 試煉場由 51 種 MVP 精簡為使用者指定的 22 種；29 種只從本地圖 Spawn 清單移除，素材與怪物資料完全保留。
- 22 隻重新平均分散，中央玩家出生點保留安全空間，降低同時進入活動視窗的 MVP 數量。
- MVP 地圖 Atlas LRU 由桌機 12/160MB、觸控 8/96MB 收緊為桌機 8/112MB、觸控 6/72MB。
- 不修改 MVP 原始能力、掉落、轉蛋、重生時間與 OFFLINE/CLOUD 邏輯。
- 無 SQL 更新。

## V0.9.87M — 雙輪健康檢查 / 穩定性精修
- Baseline: user-provided V0.9.87L full package (`roweb(20260812-160454).zip`).
- Supabase JS CDN 固定至已驗證 v2.112.3，並加入官方 unpkg 備援，降低單一 CDN / 浮動 major 版本風險。
- OFFLINE 模式補齊信箱刪除、已讀與 GM CENTER 的雲端阻擋。
- MVP / 台灣轉蛋背包 Guard 在未掛機時不再每 1.5 秒掃描整個背包。
- 統一目前版本顯示與開發基準；根目錄歷史安裝說明重新封存。
- 不需新增 Supabase SQL；J Offline Continuity、I MVP OOM、拍賣、VIP、倉庫與信箱安全機制全部保留。

# 彼岸花仙境（RO_WEB 技術核心）— 歷史紀錄

## V0.9.87K｜遊戲設定視窗精修＋拍賣入口去重

- 遊戲設定改成可拖曳、可關閉、可切換小／中／大尺寸的獨立視窗，內部可滾輪捲動。
- 視窗位置與尺寸保存在瀏覽器本機。
- 正式拍賣入口統一到第二行原本的「拍賣行」，移除第四行重複入口。
- 不需新增 SQL；V0.9.87J Offline Continuity 與既有功能全部保留。

## V0.9.87F｜拍賣三貨幣固定售價 12H

- 新上架可選 Zeny / 藍寶石 / 紅寶石。
- 上架費沿用 1 / 2 / 3 / 5 級距，改為售價同幣種支付；成交稅 5% 同幣種回收，賣家信箱領取 95%。
- 固定售價刊登 12 小時，不做競價；購買成功商品直接寄買家信箱。
- 市場新增貨幣篩選、分幣種近 7 日成交行情。
- 同一瀏覽器切換不同 Player ID 時，拍賣所有權以目前進場角色 accountId 判定。
- V0.9.87E VIP 離線掛機 15 秒/隻、倉庫救援、雲端掛機效能精修全部保留。


## V0.9.87B｜雲端掛機效能精修＋拍賣場正式入口

- 正式拍賣場入口：右上功能區第 4 行第 1 格。
- V0.9.87A 拍賣交易所、VIP V1、GM / Mail / Cloud Character 系統全部保留。
- GitHub Pages / HTTP 改載精簡 core data bundle：保留全部遊戲 `data/**` 與必要同步 Manifest，大型角色／atlas JSON 按需載入；啟動 bundle 由約 38.4 MB 降至約 13.6 MB。直接雙擊 file:// 仍載完整 bundle 保持相容。
- 掛機自動存檔採本機 2 秒合併、Supabase 30 秒最新快照同步；手動與交易關鍵操作不節流。
- 舊版本散落報告已封存至 `docs/release_history/`，根目錄維持精簡。

# V0.9.84C｜正式帳號中心＋雲端存檔同步狀態

- 玩家入口使用登入／註冊／忘記密碼正式介面，沿用角色選擇大背景。
- Email OTP 註冊、密碼重設、同 Email 最多 5 個遊戲帳號維持。
- 每帳號 12 角色槽，角色可任意移動或交換 SLOT。
- 本機角色可轉移至 Supabase，原 LocalStorage / IndexedDB 保留。
- 手動存檔會分別驗證本機耐久存檔與雲端同步狀態；雲端失敗時清楚提示但不丟失本機進度。
- 角色大廳顯示即時雲端同步狀態。
- 帳號中心新增「目前密碼 → 新密碼 → 再次確認」的登入中修改密碼流程。
- 舊 `cloud_register_test.html` 僅保留相容轉址，不再顯示測試資訊。


# V0.9.83E3｜Timed Status 自身中心快捷施放修正

補完 Generic12 輔助類中 11 招 timed_status／合唱技能的快捷施放路由。這些技能按快捷鍵時不再要求先鎖怪或自動靠近；Self-origin 範圍技以施法者為中心，仍照原技能範圍影響附近有效目標。消沈旋律／戰嚎的彼端等缺少手寫 targeting 的技能，改由官方 Self + SplashArea 資料建立範圍。12 套共用特效素材與 617 招映射不變。


# V0.9.83E2｜快捷輔助技能預設自身施放

修正快捷欄的輔助／BUFF 技能仍被舊目標距離檢查誤判為需要怪物目標的問題。Generic12 BUFF 類技能按快捷鍵時預設對自身施放；自身地面輔助落在角色腳下。敵方導向位移（弓身彈影）保留目標需求。12 套共用特效素材與技能分類不變。


# V0.9.83E1｜12 套共用技能特效完整覆蓋修正

修正 E 版 file:// 資料包未內嵌 Generic12 Manifest/Effect JSON，造成火箭術等技能完全無法載入共用特效。重新稽核所有可執行主動技能的「職業階段 × 近戰/遠程/魔法/輔助」分類；所有主動技能成功結算時固定播放一次共用特效，攻擊/敵方技能在目標怪物，BUFF/輔助在施法者。逐技能特效仍維持移除。


# RO_WEB V0.9.83E1｜12 套泛用技能特效正式替代逐技能特效

本版停止逐技能特效擴張，改用 3 個職業進程（1/2轉含進階、3轉、4轉）× 4 類型（近戰、遠程、魔法、BUFF/輔助）＝ 12 套共用特效。攻擊特效固定跟隨怪物／目標，BUFF/輔助固定顯示於施法者。舊 V92 55 招／454 個逐技能特效素材與後續 V93 逐技能方案不再部署；未來新增技能預設只做 12 類映射。


# RO_WEB V0.9.83C2｜詩舞「大暴雨」三武器相容修正

大暴雨（Skill ID 2418）依 rAthena Renewal 固定允許弓、樂器、鞭子三種武器。此次強化裝備類型正規化、裝備實例 fallback 與技能專用需求鎖，避免浪姬舞者裝備鞭子時被錯誤阻擋。弓、樂器、鞭子皆已納入回歸測試；RO_WEB 繼續沿用技能不消耗箭矢的規則。

# RO_WEB V0.9.83C1｜更新與稽核紀錄精簡

本修訂不改動遊戲內容。原本散落在專案根目錄的一百多份歷史更新、稽核、驗證與版本摘要，已完整保存於單一 `RO_WEB_HISTORY_RECORDS_THROUGH_V0.9.83C.zip`；根目錄僅保留目前版本摘要與歷史封存檔。舊工具若需要原始報告，可執行 `tools/restore_history_records.py` 還原。


# RO_WEB V0.9.83C｜四角色欄位與雲端存檔基礎

本版以 V0.9.83A 為基準，新增預設 4 格角色選擇／創角大廳。既有單角色會安全遷移至第 1 格；角色背包、裝備、技能、快捷欄、新人箱進度與地圖位置維持角色獨立，帳號共用倉庫維持共用。角色欄位上限、帳號／角色 ID、修訂序號及遠端存檔 adapter 都已預留，後續可直接接後台雲端。

登入大廳使用 1920×1080 WebP 背景與黑金半透明面板；角色卡顯示目前職業／性別對應的左上角色欄 Idle 圖、等級、所在地與最後遊玩時間。桌機顯示 4 欄，手機使用 2×2。

# RO_WEB V0.9.83A｜新人銜接裝備支援處分規則修正

以 V0.9.83 為基準，將 62 件新人支援裝備調整為**可販售、可分解，但不可存入帳號共用倉庫**。物品詳細視窗提供獨立「販售」按鈕；分解則保留指定數量流程。三階段箱子本身仍受保護，避免 100→130→160 接力被誤刪。

新人流程維持：新角色自動取得蹦級箱；既有角色可向普隆德拉中央補領 NPC 領取；依三轉／四轉職業偵測六大職業系列武器。Base Lv.100 完成後自動發放 Lv.130 幻象箱，Lv.130 完成後自動發放 Lv.160 全自動箱，每個人物各階段限領一次。

## 0.9.82IL4 台灣轉蛋完整效果稽核與報告整併
- 重新沿著獎池、物品索引、裝備插卡、卡片／套裝 Runtime、技能與受擊事件、快捷欄及掉落路徑檢查全部新獎品。
- 27321「絕望之神夢羅克卡片」保留數值效果；使用緊急治療觸發時只顯示系統訊息，不切換角色外觀。
- 28902「瘋狂兔寶寶」+12 的不斷詠標記保留，待全域受傷斷詠系統完成後直接接用。
- 4507「女王甲蟲卡片」的甲蟲次種族增傷已預先接線，新增甲蟲系列怪物後即可生效。
- 9512「光輝天翼轉蛋」維持可放快捷欄、數字鍵使用及指定數量批量開啟。
- 根目錄 TEST_REPORT／UPDATE_FILES 歷史資料重新整併為單一檔案。

# RO_WEB 0.9.82IL4

## 0.9.82IL3 絕望之神夢羅克卡片 ID 修正
- 新轉蛋的 0.01% 夢羅克卡片改為正確 ID **27321「絕望之神夢羅克卡片」**。
- 既有 ID **300084** 完整還原為「夢魘乾扁草精卡片」，不再被錯誤覆寫或放入本轉蛋。
- 27321 套用 ATK、MHP 與三種族物理增傷；依需求不切換變身外觀。
- 9512 光輝天翼轉蛋的快捷欄支援維持不變。


## 0.9.82IL2 魔王夢羅克忠誠僕人與快捷欄修正
- 新轉蛋新增 **420236「魔王夢羅克的忠誠僕人」**，單件機率 0.1%。
- 頭下、MDEF/P.ATK/S.MATK、九組搭配與台灣版阿哈特全體型效果完整接入。
- **9512 光輝天翼轉蛋可放入快捷欄**，點擊或數字鍵皆視為玩家手動開啟。
- 0.9.82IL1R：已移除誤加入的 400379；420236 尚未在本修訂加入。

本版以 0.9.82IL 為基準，完成光輝天翼轉蛋追加內容、卡片／裝備 Runtime 修正，以及快捷欄相容性修補。

## 0.9.82IL1 重點

- 光輝天翼轉蛋新增 12806「甲蟲召喚書」10%，使用後建立 30 分鐘輕量協助型甲蟲傭兵。
- IL3 已撤銷 300084 的錯誤覆寫；新轉蛋正確的夢羅克卡片為 27321「絕望之神夢羅克卡片」0.01%。
- 七件精選裝備各 0.1%、四張 MVP 卡片各 0.01%；鐵匠的祝福 5%；十種精煉材料各 8.226%，總和固定 100%。
- 天地樹的信仰＋潘利爾卡片職系條件同時支援三轉與四轉。
- 瘋狂兔寶寶魔法鏡改為觸發攻擊之後才啟用；+12「詠唱不會中斷」保留 Runtime 標記，待全域受傷斷詠完成時直接接入。
- 批量開啟型消耗品的物品詳細視窗同時顯示快捷欄選擇器，9512 可正常放入並由快捷欄使用。
- 女王甲蟲／封印女王甲蟲的 RC2_SCARABA 效果保留；目前尚未新增甲蟲系列怪物，未來補怪物次種族資料後自動生效。

後續版本以 0.9.82IL1 為基準。

# RO_WEB 0.9.82IJ

本版以 0.9.82II 為基準，恢復咒術士正式元素球玩法與屬性漩渦，並修正暴力腔棘魚因共用模板殘留魔法免疫而使魔法只造成 1 點的問題。

## 0.9.82IJ 重點

- 召喚火焰球／雷電球（風）／水球／石塊恢復為主動元素球技能；Lv1 增加 1 顆，Lv2 清除現有球並建立 5 顆同屬性球，最多 5 顆。
- 屬性漩渦至少需要 4 顆；有 5 顆時先丟棄最舊一顆，再由最新至最舊消耗 4 顆，四段各自使用球的火／風／水／地屬性，間隔 200ms。
- 釋放 Lv2 恢復消耗全部元素球的攻擊分支；Lv1 魔法書分支尚未完成時會明確阻擋，不會假裝成功或消耗資源。
- 移除舊版「不需要元素球、固定四段聖屬性」的 RO_WEB 改造。
- 2190 暴力腔棘魚強制使用 IgnoreMelee／IgnoreRanged，不再被 2189 變異腔棘魚的 IgnoreMagic 或 legacy magicImmune 欄位污染。
- 0.9.82II 已成功的攻擊特效怪物錨點、掛機恢復與連鎖電擊修正完整保留。

後續版本以 0.9.82IJ 為基準。

# RO_WEB 0.9.82II

本版修正四項實機問題：強酸禁地誤抓同種怪物或失效 DOM 導致特效跳到左上角、素質欄開啟後自動掛機永久停住、未啟用掛機卻因主動怪攻擊自動反擊，以及連鎖電擊 Runtime 類型誤判。

## 0.9.82II 重點

- 強酸禁地（水／地／風）改以精確怪物實例 `_instanceId/runtimeId/instanceId` 與事件瞬間的正式 entity.position 為權威腳底座標。
- 多隻同 ID 怪物存在時，禁止再用 `mobId/id` 直接取第一隻；無法唯一判定時暫不播放，不送往玩家或左上角。
- 怪物 DOM 僅在已連線、位於戰鬥場、尺寸有效且 instanceId 相符時作最後備援；正式世界怪物 entity.position 優先。
- 自動掛機 scheduler 使用 try/catch/finally 保證每次 tick 後重新排程；素質欄開啟與關閉都會重建掛機排程。
- 未開啟掛機時，主動怪攻擊玩家不再自動啟動連續反擊；玩家手動點怪與明確開啟掛機不受影響。
- `WL_CHAINLIGHTNING`／`WL_CHAINLIGHTNING_ATK` 統一解析至 Skill 2214 `chain_magic`，修正「Runtime 類型不符」。
- V92 55 招／454 個特效、固定飛行、Boss 即飛、死亡 UI、肯貝特、怪物屬性與精煉警告全部保留。

後續版本以 0.9.82II 為基準。

# RO_WEB 0.9.82IH

本版修正強酸禁地仍有施法／腳底圖層跟隨玩家的問題：三種強酸禁地的所有視覺階段，包含 CAST、CAST_BOTTOM、START、BOTTOM、MAIN、HIT，全部改為目標怪物腳下的世界座標快照。

### 0.9.82IH 重點

- 強酸禁地（水／地／風）不再信任可能是玩家座標或過期座標的事件 payload。
- 優先以當前目標怪物 DOM 的腳底位置建立世界座標快照。
- HIT_CONFIRM 會強制把最近 3 秒內的所有強酸禁地視覺階段重新定位到真正怪物腳底，即使舊快照曾被標記為有效。
- 技能施放與扣除成本時明確傳入 currentMonster、目標身分與目標座標。
- 玩家移動後，強酸禁地仍固定在怪物命中瞬間的位置。

## RO_WEB 0.9.82IH — Acidified Authoritative Monster Foot Anchor Fix

- 0.9.82IF 已讓主要地面環固定於怪物腳下，但原始 Manifest 的 CAST／CAST_BOTTOM 仍標記為 CASTER_BODY／CASTER_FOOT，因此自動連發時黑色星芒會持續黏在玩家腳下。
- Skill 5340／5341／5342 的 29 個非清理事件全部改為 `GROUND_CELL`，並保存原始 target 供稽核。
- Runtime 對三種強酸禁地執行硬性政策：所有視覺階段都必須使用目標怪物腳下的世界座標快照；檢查順序高於一般 CASTER 規則。
- 缺少權威目標座標時不退回玩家位置，維持等待 HIT_CONFIRM 的安全機制。
- 其他 BUFF／架式／光環仍使用玩家即時座標；一般 TARGET 命中特效與投射物規則不變。
- 後續版本以 0.9.82IH 為基準。

# RO_WEB 0.9.82IF

本版修正 V92 地面技能目標座標事件傳遞；強酸禁地會固定於目標怪物腳下的世界座標，不再使用玩家座標 fallback。

## RO_WEB 0.9.82IF — V92 地面特效世界座標快照修正

本版以 0.9.82ID 為基準，修正 V92 地面技能特效以玩家為基準保留固定偏移，造成玩家移動時特效一起移動。

- `GROUND_CELL` 與 `GROUND_SPAWN` 在事件真正觸發時取得目標怪物腳下的世界座標快照。
- 強酸禁地（水／地／風）的目標端 START／BOTTOM／MAIN／HIT 視覺全部固定在該次命中位置，不跟隨玩家或怪物。
- BUFF／架式／光環仍使用玩家即時座標；一般 TARGET_BODY／TARGET_FOOT 仍跟隨目標怪物；PROJECTILE_PATH 使用玩家與目標的即時端點。
- 世界座標快照每幀只依 Camera 轉成畫面座標，因此玩家移動導致鏡頭改變時，特效仍固定在原地圖位置。
- 0.9.82ID 的掛機、怪物屬性、精煉警告、肯貝特與 V92 被動技能 Gate 全部保留。
- 後續版本以 0.9.82IF 為基準。

## RO_WEB 0.9.82IB — V92 主動技能特效 Runtime

本版將 V91.6 Ready Library 正式接入 RO_WEB：55 招主動技能、454 個 Effect JSON、2395 張 PNG。施放特效會跟隨真實 Begin／Commit／Hit／End 事件，不再綁死固定毫秒。

- RO_WEB 已改造成被動的技能一律不進入特效部署；216 招被動技能有獨立稽核清單。
- 目前 55 招 Ready 候選全部仍為可執行主動技能，所以部署數維持 55；Runtime 每次事件仍會再次檢查 handler，未來改成被動時會立即跳過。
- BACK／FRONT 雙 Canvas 支援人物前後景；桌機 Full、手機 Min，並支援雙向 fallback。
- SKILL_END、死亡、換地圖、切角與離頁會清理 Loop。
- 不覆寫技能中文名稱或 RO_WEB 改造說明。
- 後續版本以 0.9.82IB 為基準。

## RO_WEB 0.9.82IA — Renewal 詠唱／延遲完整校正

本版以 rAthena Renewal 2026-06-18 為基準，校正玩家技能的變動詠唱、固定詠唱、技能後延遲、獨立冷卻與行走延遲。

- 變動詠唱使用最終 `DEX × 2 + INT`，530 時變動詠唱歸零；固定詠唱不受此素質公式影響。
- 修正通用與技能專屬固定詠唱百分比直接相加；通用來源取最強值，同技能的專屬來源先合併為候選值，再與通用／狀態候選取最高。
- 修正 `bonus2 bVariableCastrate,技能,-%` 可能誤變成全技能減免。
- 新增 `bSkillDelay` 技能專屬後延遲腳本支援。
- 補上八招連技的 `1000ms／DB 延遲 - 4×AGI - 2×DEX` Renewal 特殊公式。
- 技能詳細視窗新增目前裝備與 Buff 後的實際變詠、固詠、總詠唱、後延遲、獨立冷卻、行走延遲及 140ms 安全間隔。
- HZ 的分類傷害正規化、HY 批量開箱與 HX 耐久存檔完整保留。
- 後續版本請以 0.9.82IA 為正式基準。

## RO_WEB 0.9.82HZ — 種族／分類傷害鍵值正規化

本版修正卡片、裝備與被動的「對種族」加成可能沒有套用的問題。

- 新增 `js/modifier_key_runtime.js`，統一種族、體型、屬性與階級 keyed modifier 的名稱、大小寫與 rAthena 前綴。
- 人形種族統一使用 `DemiHuman`；相容 `Demihuman`、`Human`、`RC_DemiHuman`、人形與人型。
- 修正海葵卡片等 `bAddRace,RC_DemiHuman` 對資料表 `Demihuman` 無法命中的問題。
- 同步修正種族傷害、魔法種族傷害、種族耐性、種族暴擊、防禦穿透、擊殺 SP、種族 EXP、Coma 與條件式額外掉落。
- `All` 與特定分類現在可正確累加，不再因直接索引只取其中一個。
- 邪心獵人AT與既有全部人形怪物資料統一為 `DemiHuman`，並重建離線 Data Bundle。
- HY 的全域指定數量批量開啟與 HX 的耐久存檔完整保留。
- 後續版本請以 0.9.82HZ 為正式基準。

## RO_WEB 0.9.82HY — 全域指定數量批量開啟

本版將箱子、轉蛋與未來卡冊統一接入共用的「指定開啟數量」架構。

- 可在物品介紹視窗輸入開啟數量，預設 100，支援 Enter。
- 輸入量超過持有量時，只處理目前持有數量，不會產生負數或空抽。
- MVP 幸運轉蛋、神秘箱子、神秘紫箱、禮物箱、黯淡冰晶武器箱與原地復活之證箱子全部適用。
- 不使用長按連開，也不建立數千個點擊事件；大量開啟會以每批 32 次切片處理並讓出主執行緒。
- 每 256 次建立耐久檢查點，完成後立即保存；手動存檔仍會先處理／安全結束待處理箱子與轉蛋。
- 新箱子與卡冊可透過共用 Adapter 註冊，不必重新修改物品 UI。
- HX 的大量轉蛋防回檔與 HW 的每件特殊獎絕對機率完整保留。
- 後續版本請以 0.9.82HY 為正式基準。
