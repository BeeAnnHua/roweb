## RO_WEB 0.9.82HR — 全域稀有取得公告／升階材料固定 5%

本版以 0.9.82HQ 為基準，將原本只服務 MVP 轉蛋的紅／紫／金橫幅抽離成全域共用 Runtime。

### 本版重點
- 所有來源依單一物品「最終實際取得機率」判定：≤1% 紅色、≤0.1% 紫色、≤0.01% 金色。
- 已接入怪物一般掉落、MVP 額外掉落、被動技能額外掉落、卡片額外掉落、地圖額外掉落、MVP 轉蛋與全部 ItemBox。
- 加權獎池會先合併相同 Item ID 的權重，再計算物品實際機率，避免重複列造成假稀有。
- MVP 轉蛋不再直接採分類標籤；例如 1% 分類內有 5 件等權獎品時，每件實際為 0.2%，顯示紅色。
- 冰鱗山丘／蛇巢穴升階材料改為最終絕對 5%（500/10000），不套用全域 drop=10000 或舊升階材料倍率。
- 黯淡冰晶武器箱 26 等分，每件約 3.846%，因此不公告；禮物箱目前也沒有低於等於 1% 的物品。

## RO_WEB 0.9.82HP — 正式物品圖示校正版

本版以 0.9.82HO 為基準，使用 `RO_WEB專案資料/items` 的正式物品圖庫，依物品 ID 校正與補齊 `images/items`。

### 本版重點
- 專案現有數字圖示逐一與素材庫同 ID 圖示比對；同 ID 內容不同者以正式圖覆蓋。
- Item 500054「黯淡冰晶長劍」原先誤放 32×129 的非背包圖，已換成正式 24×24 物品圖示。
- 對專案物品索引、既有圖示與四組 ItemBox 獎池取聯集，補入 1,480 張缺少的正式圖示。
- 禮物箱、神秘箱子、神秘紫箱的 1,081 種獎品現在全部具有本地正式圖示。
- 1100100「黯淡冰晶武器箱」是 RO_WEB 自訂物品 ID，官方不存在同 ID 圖；保留明確映射至官方 101638 圖示，不冒用官方物品 ID。
- 後續新增物品圖示時，檔名必須使用物品正式 ID；不得以其他物品圖示複製後假裝同 ID。

## RO_WEB 0.9.82HO — RA 經典箱子版

本版以 0.9.82HN 為基準，新增三種 Ragnarok 經典箱子，使用與黯淡冰晶武器箱相同的背包開箱流程。

### 本版重點
- 禮物箱（Item 644）使用 RA `GIFTBOX` 官方加權獎池。
- 神秘箱子（Item 603）使用 RA `BLUEBOX` 官方加權獎池。
- 神秘紫箱（Item 617）使用 RA `VIOLETBOX` 官方加權獎池。
- 箱子扣除、獎勵加入、失敗歸還、背包更新與 HN 耐久存檔全部走既有正式流程。
- 獎池機率保留 RA `Rate` 權重，不會因物品資料缺失而重新加權。
- 補入 570 筆原專案缺少的獎池物品資料；當時缺少的舊物品圖示已由 0.9.82HP 正式圖庫補齊。

## RO_WEB 0.9.82HN — 防回檔耐久存檔版

本版以 0.9.82HM 為基準，優先修正長時間掛機後 F5／重新開頁可能回到舊進度的問題。

### 本版重點
- 主存檔與安全備份均在寫入後重新讀回驗證。
- 載入不再固定先讀主檔，而是比較主檔、備份、IndexedDB 與未來後端候選，選擇最新有效版本。
- 每 60 秒保存，並在頁面隱藏、F5、關頁、凍結前同步補存。
- 最新分頁擁有唯一寫入權，舊分頁不能覆蓋較新的掛機成果。
- IndexedDB 保存最新與上一代快照，localStorage 異常時仍有第二層復原來源。
- 後端同步接口已預留；目前沒有後端時完全以離線本機模式運作。
- 測試倍率統一：Base EXP／Job EXP／Drop／Zeny／Card／Map Exclusive／Grade Material 全部為 `10000`。

## RO_WEB 0.9.82HM — 貨幣列原地展開修正版

本版以 0.9.82HL 為正式基準，專門恢復右上貨幣欄原本的「點擊後在原位置展開完整數量」操作。

### 本版重點
- 不再依賴額外的「持有貨幣」浮動視窗，直接在既有右上 `#top-bar` 原地展開。
- 收合狀態維持 Zeny／藍寶石／紅寶石三格精簡排列。
- 點擊整個貨幣欄後，改為三列完整顯示名稱與完整數字；再點一次、點擊外部或按 Escape 即可收合。
- 使用 document capture 的 `pointerup`／`click`、HTML inline onclick 與鍵盤 Enter／Space 三重入口，避免早期 HUD 點擊程式攔截。
- 完整數字使用獨立顯示節點，因此角色 UI 更新時不會被舊的精簡數字重繪覆蓋。
- 保留 0.9.82HL 已成功的貨幣滑鼠提示，以及消耗品／裝備／一般道具 Tooltip 修正。
- 保留 HH～HL 的附魔、升階、地圖滾輪、NPC 玩家文字與正式出生背包規則。

歷史更新／測試／稽核紀錄已續寫至 `HISTORY_UPDATE_AUDIT_LOG_THROUGH_0.9.82HL.txt`。

### 從 0.9.82HL 使用增量包更新
覆蓋檔案後，請在專案根目錄執行一次 `APPLY_0.9.82HM_CLEANUP.bat`，移除上一版留在根目錄的 HL 報告與清理腳本。完整包不需要執行。
