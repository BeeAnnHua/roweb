彼岸花仙境 V0.9.86K — 殘影追蹤救援（累積 H / I / J）

安裝：
1. 將本 Patch 內檔案覆蓋到目前 V0.9.86J 專案。
2. GitHub Pages 更新後 Ctrl+F5。
3. V0.9.86K 不需要新增 SQL；沿用已執行的 V0.9.86I_LEGACY_BROWSER_CHARACTER_RESCUE.sql。

本版重點：
- 完整保留 H/I/J：localStorage + IndexedDB、sessionStorage、巢狀 JSON 深掃、完整 Email 顯示、人工確認、跨帳號 UUID 阻擋。
- 新增「不完整舊角色線索」區，不把殘缺摘要誤當成可復原角色。
- 顯示 SLOT / 名稱 / 職業 / Base / Job / Character ID / 完整度。
- 依 Character ID / 名稱反向搜尋 localStorage、sessionStorage、IndexedDB 已掃描資料列，列出最多 5 個追蹤來源。
- 殘影線索只讀，不會自動建立角色、不會覆蓋雲端、不會刪除原始資料。

目前 100011 測試目標：
- SLOT 2 / 3 / 4 的三個完整候選應繼續存在。
- 若 SLOT 1 十字軍只剩 profile/slot 索引，K 版應在「不完整舊角色線索」區顯示它。
- 測試時仍先不要確認復原；請截完整候選 + 不完整線索畫面，再決定下一步。
