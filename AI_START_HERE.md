# RO_WEB 0.9.82HY

最新基準：全域指定數量批量開啟，以及 HX 耐久手動存檔／轉蛋防回檔。

- 最新正式基準：0.9.82HY。
- 箱子、MVP 轉蛋、未來卡冊共用 `ItemBatchOpenRuntime`；不得為單一道具另寫批量 UI。
- UI 只提供數量輸入與開啟按鈕，不做長按連開。
- 大量開啟必須切片處理、每 256 次檢查點、完成後保存，避免主執行緒卡死。
- 手動存檔必須先處理共用待處理獎勵，再驗證 localStorage／IndexedDB 最新快照。
- 後續版本一律以 0.9.82HY 為基準。
