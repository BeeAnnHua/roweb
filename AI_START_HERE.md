## 0.9.82HF — 最新基準
- 最新基準為 0.9.82HF，完整繼承 HE 的地圖按鈕精簡與控制列對齊。
- HF 僅縮短桌機長怪物清單視窗並增加第一分類標題上方留白；手機版維持 HE。
- 後續更新必須從 HF 開始，不可回退地圖觸控防穿透與桌機／手機導覽分流。

## 0.9.82HE — 最新基準
- 最新基準為 0.9.82HE，完整繼承 HD 圓形視窗控制與裝置分流。
- 地圖掉落詳情的所有導航按鈕固定於頂部：手機為「進入地圖／返回」，桌機為「返回怪物清單」。
- 掉落詳情底部不再生成重複返回按鈕。
- 地圖視窗大／中／小與關閉按鈕使用同一垂直中心線。

## 0.9.82HD — 最新基準
- 最新基準為 0.9.82HD，完整繼承 HC 手機地圖導覽、HB 觸控防穿透與 MVP 卡片來源、GY 黯淡冰晶附魔。
- 地圖掉落 UI 已依輸入裝置分流：桌機僅需返回怪物清單；手機保留進入地圖／返回地圖。
- 標準遊戲視窗的 window-close 必須維持原始圓形圖片；不可再以通用黑金方形按鈕覆蓋。
- index.html 的 CSS／JS query version 必須與 RO_WEB_VERSION 同步，避免 GitHub Pages 快取混載。

## 0.9.82HB — 手機掉落頁防穿透與卡片來源合併
- 最新基準為 0.9.82HB，完整繼承 HA 雙頁地圖怪物／掉落檢視與 GY 黯淡冰晶附魔。
- 粗指標／觸控裝置不得用 pointerenter、hover、focus 開啟地圖怪物面板；只接受正式點擊。
- 觸控怪物列與返回必須使用穩定 pointer activation，阻擋 ghost click 與底層地圖按鈕事件穿透。
- 手機怪物面板開啟期間是模態層；底層 map-template-body 其他內容不得接收 pointer events。
- 詳情頁頂部固定「返回」，長掉落表底部另有「返回怪物清單」。
- `loadMonsterData()` 必須把 `data/card_runtime/card_drop_sources.json` 合併至現有 monster.drops；用 monsterId 優先，必要時以去除尾端底線的 AegisName 對應，並以 itemId 去重。
- 卡片掉落必須走正式 `cardDrop` 與全域掉寶倍率，不可只補 UI 顯示。
- MVP 試煉場：49/51 隻有官方卡片來源，共 51 筆；夜勝魔、失落之龍不可自行補不存在的卡片。

# 目前基準：RO_WEB 0.9.82HB

## 0.9.82HA — 地圖怪物／掉落雙頁檢視修復
- 最新基準為 0.9.82HA，完整繼承 GZ 每分鐘備份、手機素質欄與所有 GY 黯淡冰晶附魔功能。
- 地圖怪物查詢是雙頁狀態機：list → detail → 返回 list。怪物清單與掉落詳情不得同時顯示。
- 手機／觸控版必須將查詢面板放在 `#map-window .map-template-body` 上方作全區覆蓋，不得重新塞回 `.map-current-card`。
- 詳情頁右上角固定為「返回」，不得觸發地圖傳送、關閉地圖窗或重建整個地圖 UI。
- MVP 試煉場 51 隻怪物必須只在固定面板內捲動，不能以增加整體高度解決。

## 0.9.82GZ — 每分鐘備份、手機素質欄與怪物掉落查詢修復
- 每 60 秒同步更新角色主存檔與一份獨立安全備份；主存檔損壞時可自動改讀最近備份。
- 頁面關閉／切換時同步刷新主存檔與備份；清除角色資料時備份亦會一併移除。
- 手機／觸控裝置的素質配點窗最大寬度提高至 560px，左右欄與 STR～LUK 配點格、數值與「＋」按鈕同步加寬。
- 修正手機地圖傳送清單點選怪物後，掉落詳情未固定或視窗回彈的問題；點擊後會固定怪物並自動捲至掉落詳情。
- 桌機與手機的怪物分布／掉落查詢可視高度與捲動範圍加長，怪物很多的地圖也能完整查看。

# 目前基準：RO_WEB 0.9.82GY

## 0.9.82GY 核心
- 黯淡冰晶武器正式附魔：第4洞→第3洞→第2洞，第1洞卡片。
- 26 把武器、189 項附魔、8 條物理／魔法升階、12 條 EP19 兌換配方。
- 正式扣材料、寫入 instance、CardRuntime 套用效果、存檔與倉庫保存。
- 蛇巢穴四種蠕蟲：花葉1%、乙太星塵1%、武器箱0.10/0.15/0.20/0.30%，皆為一般掉落且吃全域倍率。
- 毒氣魔力 1001033 尚未指定取得來源，不可擅自新增。
- 缺任何新圖片必須停止該部分並回報，不可替代。

本版建立全專案共用附魔平台憲法與斐揚 UI 預覽。任何後續附魔裝備必須沿用第1洞卡片、第4洞 → 第3洞 → 第2洞順序，以及同一套左側裝備／中央洞位／右側候選／下方材料與確認按鈕架構。`js/enchant_platform_preview.js` 目前僅為固定示意資料，不得誤認為已完成黯淡冰晶附魔 Runtime；下一版才接入裝備偵測、材料、Zeny、效果、升階、重置與存檔。

# 目前基準：RO_WEB 0.9.82GV

本版新增安全數量分解：背包批次分解與物品資料窗分解都必須先選擇數量並確認。預設分解 100 件，大型堆疊只扣確認數量；運算採單次陣列重建與單次存檔。鎖定、穿戴中裝備與 MVP 幸運轉蛋不可分解。

# 0.9.82GT 最新基準（2026-07-28）

- 下一次修改必須以 0.9.82GT 為唯一基準；本版完整繼承 0.9.82GS 的 MVP 分散出生、固定戰鬥席位與怪物分離推力。
- MVP 幸運轉蛋 ID 14848 為手動確認型消耗品：背包格單擊只顯示資料，不得直接開啟。
- 轉蛋只能由物品資料「使用」按鈕或玩家主動快捷欄操作送入 `MvpGachaRuntime`；任何背景程式或通用 `consumeItem()` 呼叫必須阻擋。
- 掛機期間 `MvpGachaRuntime` 每 500ms 核對轉蛋數量；未授權減少必須自動復原。合法手動開啟與葛坡尼亞 MVP 掉落會同步更新守衛基準。
- 自動 HP／SP 補品、異常解除、商城料理與其他掛機消耗流程不得把轉蛋列為候選。
- 技能名稱仍以台服 SkillInfoz 為準；技能效果以 RO_WEB Runtime 為唯一權威。EP19 正式怪物動畫與卡片 Runtime 不得回退。

## 0.9.82GQ — 零背景掃描黑金 UI 統一

- 全站操作按鈕、確認／取消、轉職、精煉、升階、合成、倉庫與數量輸入統一黑金風格。
- 移除黑金主題的全域 MutationObserver；動態按鈕由靜態 CSS 直接套用。
- 自訂數量控制器僅在指定欄位建立時初始化一次；自動掛機欄位維持專用控制器。
- 共用確認視窗只保留單一 DOM 節點，不加入背景輪詢或遊戲 Tick。

## 0.9.82GP — 樞機主教持續祈療存檔遞迴修正

- 修正樞機主教／神官四轉在「持續祈療」Buff 存檔後重新載入時，週期治療於能力值彙總內再次計算衍生 MATK，造成 `Maximum call stack size exceeded` 並使遊戲啟動失敗。
- Buff 能力值彙總改為純讀取：可清除過期 Buff，但不執行週期治療、SP 扣除或脈衝效果；正常週期效果仍由遊戲 Tick 執行。
- 玩家存檔載入期間不補跑離線期間的週期治療，避免啟動階段副作用。
- 隔離自動掛機專用數字控制器與全域黑金數字控制器，避免大量四轉 Buff 設定產生雙重包裝。

## 0.9.82GN 屬性附加同層互覆與技能強制屬性

- 肯貝特、暗水、灑水、塗毒、賢者附加等武器屬性來源共用單一槽，物品與技能同優先，最後施加者覆蓋。
- 鎧甲屬性物品／技能使用獨立單一槽，最後施加者覆蓋。
- 更換或卸下主手／刺客副手清除武器屬性；更換或卸下鎧甲清除鎧甲屬性。
- 技能本身指定的強制傷害屬性優先於武器槽，但不會刪除槽內 Buff。
- 新增暗水（12020）資料與原始物品圖，20 分鐘賦予暗屬性。


## 0.9.82GH 帳號共用倉庫與分層刪除
- 普隆德拉新增「卡普拉倉庫管理員」，提供 100 格帳號共用倉庫。
- 一般物品以堆疊保存；裝備保留 instanceId、精煉值、卡片、附魔、階級與損壞狀態。
- 已穿戴裝備必須先卸下；鎖定物品不可存入。角色刪除時不會自動搬運背包或裝備。
- 「清存檔」改為兩種選擇：只刪除角色並保留倉庫／永久刪除角色與倉庫全部資料。全部刪除必須輸入「全部刪除」二次確認。
# RO_WEB 0.9.82GG

## 精煉系統契約
- 精煉 NPC 固定為 `payon_refine_npc`，位於 `cityId: payon`；NPC 與所有 UI 都呼叫 `js/refine_runtime.js`。
- `data/refine_rules.json` 是唯一精煉規則來源，必須由 rAthena Renewal 2026-06-08 `db/re/refine.yml` 產生；不得在 UI 或 Runtime 另寫一套成功率。
- 完整支援 Weapon Lv1～5、Armor Lv1～2、目標 +1～+20，以及 Rate、Price、Material、BreakingRate、DowngradeAmount、BlacksmithBlessingAmount、BroadcastSuccess／Failure。
- 鐵匠的祝福 ID 6635 只能在規則要求數量大於 0 時放入紅框；失敗時阻止損壞與退階，但礦石、Zeny 與祝福照常消耗。
- 精煉實例值儲存在裝備 instance 的 `refine`，不得寫回共用 item master。`EffectRuntime` 與 `RARenewalDamagePipeline` 必須透過 `RefineRuntime` 讀取裝備實例。
- Lv5 武器每精煉 +1 額外 P.ATK／S.MATK +2；Lv2 防具每精煉 +1 額外 RES／MRES +2；其餘 Bonus／RandomBonus 直接讀 RA 規則。
- 轉蛋 10,000 基點：稀有 121、補品／料理 3,179、精煉材料 6,700。6635=500；7619、7620、6240、6241 各 800；1000331～1000336 各 500。
- 手機角色資訊必須保留 `mobilePlayerJob`、`mobilePlayerId`、`mobilePlayerIdEditButton` 三個 DOM，排列為職業→ID→更改 ID→等級。

## GG 不可回退契約
- 不得把精煉成功率改成自訂簡化表，也不得只支援 +10。
- 不得直接使用客戶端整張精煉 UI 圖；介面維持原創 HTML／CSS，僅使用物品圖示。
- 不得讓祝福在規則未開放的階段使用，也不得在材料不足時先扣除部分資源。
- 每次修改轉蛋必須驗證普通池 9,879 + 稀有池 121 = 10,000。

# RO_WEB 0.9.82GE

## 死侍武器自動前置技能契約
- `js/auto_battle.js` 的 `resolveAutoSkillPrerequisite()` 是自動戰鬥目標狀態前置解析入口；目前 `servant_sign` 對應技能 5203。
- 5204「死侍武器-瞬幻」與 5205「死侍武器-破滅」缺少目前目標的 `servant_sign` 時，必須先回傳 `prerequisiteSkill`，標記成功後才於下一個戰鬥動作施放原技能。
- 前置技能不得推進 `attackRotationCursor`；切換目標時必須依新目標狀態重新判定。
- 自動前置失敗不得消耗原技能 SP、冷卻、攻擊動作或洗系統訊息；手動施放仍應保留缺少標記提示。
- 5204／5205 的 `autoPrerequisiteMinimumRemainingResource` 固定為 1；自動標記不得消耗最後一個劍體。
- `previewRuntimeResourceCost()` 對 `mode: up_to` 必須使用 `minimum` 作為最低門檻，不可把 `amount` 誤當固定需求。
- 5203 必須維持 `statusAffectsBoss: true`，以符合 rAthena 對 Boss／MVP 可標記的行為。

## GE 不可回退契約
- 不得把 5203 直接加入玩家的四格攻擊技能輪替；它是由前置解析器在必要時插入的一次動作。
- 不得在自動戰鬥缺少印記時呼叫原技能後再依錯誤訊息重試；應在技能結算前完成狀態檢查。
- 不得讓上一隻怪物的印記狀態影響新鎖定目標。

# RO_WEB 0.9.82GD

## 玩家 ID／公告／轉蛋／商城料理
- 角色資訊卡不得重新加入性別文字或本地切換按鈕；首次男女選擇與普隆德拉「角色造型師」是保留的性別外觀入口。
- `player.name` 現為玩家自訂 ID，`player.playerIdVersion` 為遷移標記；角色卡顯示「職業 玩家ID」，公告名稱統一讀取 `getPlayerAnnouncementName()`。
- 轉蛋稀有大獎顯示玩家 ID；只有 MVP 的直接卡片掉落使用紅色橫幅，一般怪物卡片不得觸發。
- `data/mvp_gacha.json` 使用 10,000 基點母池：稀有 121、既有補品／料理 3,379、新增精煉材料 6,500，總合必須固定為 10,000。
- 精煉材料固定機率：6635=500、6240=1500、6241=1500、1000331～1000336 各 500。
- 商城能力料理必須具有 `cashFoodEffect`；百分比補品必須具有 `percentHeal`，並由 `MvpGachaRuntime` 實際消耗與套用。
- 自動掛機料理清單只接受背包中具有 `cashFoodEffect` 的物品；同效果仍有效時不得重複消耗，清單順序即優先順序。
- 自動戰鬥視窗必須保留 `.auto-combat-scroll` 內部滾動；料理清單另有 190px 最大高度。
- 目前啟用物品 1,711 筆、ITEM 圖片 805 張；新增物品後必須同步資料、圖示與 `js/data_bundle.js`。

## GD 不可回退契約
- 不得把玩家 ID 與角色職業混成同一存檔欄位；職業仍以 `jobKey`／`job` 為準，ID 只作顯示與公告。
- 不得讓普通怪物掉卡觸發 MVP 橫幅，也不得讓未設定 ID 的角色公告出現空白，fallback 使用「冒險者」。
- 轉蛋每次修改後必須重新稽核稀有與普通權重總和為 10,000。
- 自動料理不得在效果尚未結束時連續吃道具，也不得將百分比補品誤列為能力料理。

# 0.9.82GC 歷史基準

## 角色性別／卡片分類／ITEM 圖片瘦身
- 全新角色或清除存檔後，首次進入必須先選擇男性／女性初學者；選擇完成前不繼續啟動。
- 既有存檔自動沿用或推斷原本性別，不強迫重選；性別只控制人物 Atlas，不得改寫職業。
- 角色介面與普隆德拉「角色造型師」共用同一套免費性別切換 Runtime，只更換動畫外觀，完整保留等級、技能、素質、裝備、卡片、背包、金錢及地圖進度。
- 吟遊詩人／舞孃家族不再受角色性別限制；缺少該性別專用 Atlas 時，只借用對應職業外觀，實際職業維持不變。
- 卡片稀有度改由官方本體／同族來源判定，忽略無關活動 MVP 複製怪；瘧蚊卡片等 7 張誤分類已校正。
- 波利卡片 4001 固定使用原始卡片圖；ITEM 圖片目錄只保留目前 1,702 筆啟用物品實際引用的 796 張圖。

## GC 不可回退契約
- `data/player_default.json` 的全新角色必須維持 `gender: null`、`genderChosen: false`。
- `js/character_gender.js` 是開場選擇、角色介面與 NPC 切換的唯一共用入口。
- 性別切換不得重建角色、不得重置任何養成資料，也不得以性別自動改職業。
- 卡片金色／Boss／普通邊框必須讀取 `cardVisualTier`；不得再用所有掉落來源中任一 MVP 直接判定。
- 新增物品時必須同步加入正確 `icon` 路徑，再由 ITEM 圖片稽核／瘦身工具保留實際使用圖。

# 0.9.82GB1 歷史基準

## 龍騎乘緊急熱修
- 「龍駕馭」維持被動技能，不改成主動切換。
- 「騎乘術」恢復為劍士家族共用的主動上下坐騎技能。
- 盧恩騎士／盧恩龍爵使用騎乘術時，依目前職業自動解析並上下龍。
- 龍坐騎要求騎乘術 Lv1 與龍駕馭 Lv1 以上。
- 召喚物控制保留為第二個上下龍入口。
- 因轉職要求先解除坐騎，三轉角色可再次正常下龍後轉職。


## 0.9.82GB — 近戰範圍／自動輪替／風鷹共存／屬性與掉落總閥
- 自身中心攻擊技能不再受鎖定目標距離阻擋；官方 splashArea 自動轉為實際 AoE。
- 騎士、十字軍、刺客、流氓、鐵匠等近戰系列的既有範圍技能統一套用官方範圍資料。
- 自動掛機攻擊技能 1～4 成功施放後輪替，不再固定優先第一格。
- 菁英狙擊與憤怒暴風可共存；保留毀滅風暴暴擊與漸進狙擊增傷互動。
- 肯貝特只覆蓋普通攻擊／武器屬性技能；技能固定屬性優先，卸下武器立即解除附魔。
- drop 成為一般掉落、卡片與葛坡尼亞轉蛋的全域總閥，另提供卡片與地圖限定相對倍率。
- MVP 幸運轉蛋改用 ITEM 9525 圖示。
# 0.9.82GB 歷史 Runtime
歷史版本：**0.9.82GB**。  
基準為 0.9.82FZ；不得回退 FZ 的葛坡尼亞 MVP 試煉場、地圖限定轉蛋與時光超越裝備資料，也不得回退 FY 的 UI 效能、FX 的卡片／裝備／Combo 統一效果 Runtime，以及 FV/FW 的插卡／拆卡、掛機資源不足 15 秒防卡死與四轉 BaseHP／BaseSP 修正。

## GA 技能／屬性／掉落契約
- 自身中心範圍攻擊以玩家為中心，鎖定怪物僅可提供方向，不得以目標距離阻擋施放；目標型技能仍必須檢查射程。
- `skill_db` 的 `splashArea` 是圓形／方形 AoE 的官方最小半徑；舊 Runtime 半徑較小時必須擴展至官方值。
- 自動掛機攻擊欄 1～4 成功施放後採 round-robin 輪替；技能不可施放時可跳至下一格。
- 技能固定屬性高於肯貝特武器附魔；肯貝特只作用於普通攻擊與 `elementSource: weapon` 技能，卸下武器立即解除。
- `server.rates.drop` 是一般掉落、卡片與葛坡尼亞轉蛋的共同總閥；`cardDrop`／`mapExclusiveDrop` 是 100=1 倍的分類相對倍率。
- MVP 幸運轉蛋圖示使用 ITEM 9525；內部稀有母池機率不受掉寶倍率影響。

## FY UI 效能契約
- `updatePlayerUI()` 只負責高頻 HUD 更新；不得再於每次攻擊、扣血、回復或 EXP 變化時無條件重建素質、進階屬性、技能或職業視窗。
- `status-window` 關閉時，必須取消待處理的素質更新，背景不得建立 `status-advanced-panel` DOM。
- 進階屬性只在角色衍生值、裝備實例／卡片、Buff 或頁籤／展開狀態真正改變時更新。
- 進階屬性滾動期間必須延後重繪，並保留 `scrollTop` 與所有 `<details>` 展開狀態。
- 技能欄與職業欄隱藏時不得重建；由 `ui.js` 在視窗開啟時立即刷新。
- 快捷欄必須使用內容簽章跳過相同內容的 DOM 重建。
- 手機／觸控環境不得對持續動畫的世界背景套用 UI `backdrop-filter`；進階屬性面板全平台禁用背景模糊。

## FX 統一效果契約
- `js/card_runtime.js` 是 rAthena 裝備／卡片／Combo Script 的唯一解析與事件執行入口；`js/effect_runtime.js` 是人物素質、戰鬥、恢復、技能時序與政策效果的共用來源入口。
- 現有資料使用的 **142 種 rAthena bonus 指令**全部有解析器與實際消費端；910 張卡片、141 件啟用裝備 Script、784 組 Combo 共用同一套 Runtime。
- 新增裝備或卡片只要在主資料提供 `scriptRaw`、`Script`、`script`、`equipScript` 或 `compiledScript`，即可由 Dynamic Script Fallback 進入統一管線。
- 遇到未支援的新指令不得忽略：Runtime 必須記錄 diagnostics、console error，完整性測試與發行稽核必須失敗。
- 最終能力值及素質面板只讀取 `calculateDerivedPlayerStats()`；卡片、裝備、Combo、被動技能與 Buff 不得建立分離計算副本。

- 召喚物控制視窗改為統一狀態面板：坐騎／龍、獵鷹、狼、生命體、元素精靈及獨立召喚物皆會顯示，不再開啟空白。
- 狼與獵鷹可同時存在；移除互斥與自動解除，並同步修正技能說明。
- 盧恩騎士／盧恩龍爵三轉後不再透過二轉騎乘術切換龍；龍駕馭解鎖後於召喚物控制切換。


## 0.9.82GK 裝備升階／冰鱗山丘／蛇巢穴
- 新增 134～142 冰鱗山丘、143～151 蛇巢穴。
- 怪物原始掉落完整保留；乙太星塵與基礎寶石只補缺，使用獨立 Bonus Drop 並自動避開重複。
- 斐揚新增裝備升階匠人：五級武器／二級防具無階→D→C→B→A。
- 成功率、材料、Zeny、破壞／安全選項與庇佑乙太星塵依 rAthena 2026-06-08。
- 升階材料完成品透過 NPC 合成，消耗材料與 Zeny。


## 0.9.82GL 穩定性／介面擴充
- Server Config 新增 gradeMaterialDropRate（100=1倍），只控制新地圖額外升階材料。
- 傳送介面怪物清單支援按需查詢原始、MVP 與升階材料掉落，不接入掛機 Tick。
- 修復裝備升階視窗無法開啟與資料尚未完成載入時的錯誤處理。
- 所有村莊新增共用倉庫 NPC，容量 200 格，新增消耗／裝備／道具分類。
- 背包顯示提升為 5×6（30 格）。
- 一般／特性素質配點改為合併刷新與延遲存檔，保留掛機目標與控制器。
- 修正魅影追蹤者虛假炸彈公式；抄襲／繁殖技能豁免原職業專屬戰鬥資源。


## 0.9.82GM 介面與城鎮修正版
- 背包 5×6 的視窗、面板、格子清單與操作列同步加高，修復爆版。
- 素質配點下一動畫幀立即更新，存檔合併延遲縮短為 100ms，掛機控制器保持不重建。
- 裝備升階 NPC 改為明確呼叫 Runtime；無裝備也必定開窗，另提供「材料合成」直達按鈕。
- 修正斐揚誤用吉芬背景素材，並加入城鎮背景快取隔離。


## 0.9.82GN 斐揚素材／背包線框／升階視窗修正版
- 換入使用者提供的斐揚 1280×720 透明原圖，輸出 WebP 背景與 320×180 縮圖。
- 刪除錯誤 GM 斐揚版本圖與已退役的妙勒尼單格背景／縮圖。
- 修復背包 5×6 格子金色邊線與常駐捲動軌道。
- 修復升階 CSS 中被寫成文字的 `\n`，並加入 inline modal fallback；無裝備也能開啟升階頁，材料合成可直接開啟。

## 0.9.82GX — 完整附魔目錄與綜合材料兌換預覽
- 以 0.9.82GW 為基準，維持純預覽模式，不偵測背包、不扣材料、不寫入裝備。
- 黯淡冰晶附魔平台匯入 RA Enchant ID 47 完整資料：第4洞 179 項、第3洞 99 項、第2洞物理／魔法 Lv.1 各 1 項。
- 匯入物理／魔法 Lv.1→Lv.5 共 8 條升階路線與正確材料數量；重置維持專案規則雪花魔力原石 ×5。
- 附魔清單加入名稱、技能與 Item ID 搜尋，所有職業附魔一律顯示。
- 斐揚新增「綜合材料兌換研究員」，固定顯示全部材料，不依背包持有量過濾。
- 目前來源未提供完整提煉比例，兌換按鈕維持停用；不猜測正式配方數量。
- 本階段共核對 209 張必要圖片，缺圖 0 張。
