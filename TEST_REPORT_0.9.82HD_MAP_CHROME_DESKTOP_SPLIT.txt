RO_WEB 0.9.82HD — 地圖視窗圓形關閉鈕與桌機／手機操作分流測試報告
================================================================================
基準版本：0.9.82HC
完成版本：0.9.82HD
日期：2026-07-29

一、修正內容
--------------------------------------------------------------------------------
1. 修正 HC 地圖標題列將 window-close 設為 position:relative，造成關閉鈕位置偏移。
2. 全部標準 game-window 的右上角關閉鈕恢復 btn_close_round.png 圓形圖片。
3. 地圖視窗尺寸按鈕固定在右上角關閉鈕左側；關閉鈕固定為 28×28。
4. 桌機怪物清單頁不顯示「進入地圖／返回地圖」。
5. 桌機掉落詳情頁只保留上方與底部「返回怪物清單」。
6. 手機／觸控版維持「進入地圖／返回」及底部「返回怪物清單／返回地圖」。
7. index.html 的 title、CSS、43 份 JS 與預載圖片 query version 全部由舊 HB 修正為 HD，避免 GitHub Pages 新舊快取混載。

二、實際 Chromium 互動測試
--------------------------------------------------------------------------------
PASS  桌機 1280×720：怪物清單內進入地圖按鈕數 0。
PASS  桌機 1280×720：掉落詳情內返回地圖按鈕數 0。
PASS  桌機 1280×720：掉落詳情只有 2 個返回怪物清單（頂部＋底部）。
PASS  桌機地圖關閉鈕 computed position=absolute、28×28、border-radius=50%。
PASS  桌機地圖關閉鈕背景為 btn_close_round.png。
PASS  一般背包視窗關閉鈕亦恢復圓形圖片。
PASS  地圖尺寸按鈕位於關閉鈕左側，兩者沒有重疊。
PASS  手機 390×844：清單頁保留進入地圖＋返回。
PASS  手機 390×844：詳情頁保留進入地圖、返回怪物清單與返回地圖。
PASS  HC 既有觸控拖曳、關閉、進入地圖與 Safari ghost-click 回歸測試。

三、專案稽核
--------------------------------------------------------------------------------
PASS  JavaScript 語法：758 / 758。
PASS  JSON 解析：2,047 / 2,047。
PASS  深度健康檢查：0 errors / 0 warnings。
PASS  index.html 資源快取版本僅有 0.9.82HD。
PASS  RO_WEB_VERSION = 0.9.82HD。

四、安全邊界
--------------------------------------------------------------------------------
- 本版未修改怪物資料、掉落率、MVP 卡片來源、附魔、戰鬥公式或玩家存檔格式。
- 手機版既有觸控防穿透與三層導覽保留。
- 桌機只精簡查詢按鈕，不改變點擊怪物與掉落資料內容。
